steps:
  # Docker Build
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/sensitive-detection/myimage:$SHORT_SHA', '.']
  
  # Docker push to Google Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/sensitive-detection/myimage:$SHORT_SHA']
  
  # Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
    - 'run'
    - 'deploy'
    - 'sensitive-image-detection'
    - '--image=asia-southeast1-docker.pkg.dev/$PROJECT_ID/sensitive-detection/myimage:$SHORT_SHA'
    - '--region=asia-southeast1'
    - '--platform=managed'
    - '--allow-unauthenticated'
    - '--memory=8Gi'
    - '--cpu=2'
    - '--timeout=3600'

options:
  logging: CLOUD_LOGGING_ONLY

images:
- 'asia-southeast1-docker.pkg.dev/$PROJECT_ID/sensitive-detection/myimage:$SHORT_SHA'